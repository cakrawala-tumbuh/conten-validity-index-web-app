/**
 * Setup file untuk Jest yang dijalankan setelah framework testing siap.
 *
 * Mengimpor `@testing-library/jest-dom` untuk menambahkan custom matchers
 * seperti `toBeInTheDocument`, `toHaveTextContent`, dll.
 */
import "@testing-library/jest-dom";
